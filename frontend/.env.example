# Optional: full URL to API when the app is not served with Vite dev proxy
# Leave unset during local dev (Vite proxies /api → http://localhost:4000)
# Production build: set to your API base URL (no trailing slash), e.g. https://api.yourapp.com
# VITE_API_URL=https://your-api.example.com
