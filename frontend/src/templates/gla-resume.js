const glaResume = {
  id: "gla-resume",
  name: "GLA Resume",
  badge: "ATS",
  /** Single-column ATS layout: `template-editor-live.html` (`layoutVariant: gla-ats`). Wizard → “GLA Resume”. */
};

export default glaResume;
