const liveEditor = {
  id: "live-editor",
  name: "Live Editor",
  badge: "NEW",
  /** Classic live canvas; use wizard “Live — Split” or template `live-editor-split` for teal sidebar */
};

export default liveEditor;
